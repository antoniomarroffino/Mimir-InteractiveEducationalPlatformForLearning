%dev.quarkus.mongodb.connection-string=mongodb://localhost:27017
%dev.quarkus.mongodb.database=myAppDev
%prod.quarkus.mongodb.connection-string=${MONGO_URI}
%prod.quarkus.mongodb.database=myAppProd
%prod.quarkus.mongodb.ssl.enabled=true
%prod.quarkus.mongodb.ssl.invalid-hostname-allowed=true
quarkus.hibernate-validator.fail-fast=true
quarkus.oidc.auth-server-url=https://login.microsoftonline.com/${AZURE_TENANT_ID}/v2.0
quarkus.oidc.client-id=${AZURE_CLIENT_ID}
quarkus.oidc.application-type=service
quarkus.http.auth.permission.authenticated.paths=/api/secure/*
quarkus.http.auth.permission.authenticated.policy=authenticated
quarkus.oidc.token.issuer=https://login.microsoftonline.com/${AZURE_TENANT_ID}/v2.0
quarkus.http.cors=true
%dev.quarkus.http.cors.origins=http://localhost:3000
%prod.quarkus.http.cors.origins=${CORS_ORIGINS}
quarkus.http.cors.headers=accept, authorization, content-type, x-requested-with
quarkus.http.cors.methods=GET, POST, PUT, DELETE, OPTIONS
quarkus.http.cors.exposed-headers=*
quarkus.http.root-path=/api
quarkus.http.host=0.0.0.0
app.admin.names=Admin
app.admin.emails=admin${AZURE_DOMAIN_NAME}
app.azure.tenant-id=${AZURE_TENANT_ID}
app.azure.client-id=${AZURE_CLIENT_ID}
app.azure.client-secret=${AZURE_SECRET}
app.azure.graph.scope=https://graph.microsoft.com/.default
quarkus.smallrye-openapi.path=/q/openapi
quarkus.swagger-ui.always-include=true
quarkus.smallrye-openapi.store-schema-directory=target/classes/META-INF/resources
quarkus.mongodb.connection-timeout=60000
quarkus.mongodb.read-timeout=60000
quarkus.mongodb.max-connection-idle-time=60000
